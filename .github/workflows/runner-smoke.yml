name: Runner Smoke
on: pull_request
jobs:
  smoke_full:
    runs-on: [self-hosted, macOS, code-review]
    steps:
      - run: echo "full-labels ran on $(hostname) as $(whoami)"
  smoke_min:
    runs-on: self-hosted
    steps:
      - run: echo "self-hosted-only ran on $(hostname)"
