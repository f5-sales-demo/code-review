name: Ephemeral Smoke
on: pull_request
jobs:
  smoke:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: [self-hosted, macOS, code-review]
    steps:
      - run: echo "ephemeral runner OK on $(hostname) as $(whoami)"
